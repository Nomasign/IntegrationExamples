{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "NomaSign": {
    "BaseUrl": "https://integration.nomasign.com",
    "ClientId": "nomasign-integration"
  },
  "KeyVault": {
    "Url": "",
    "SecretPrefix": ""
  },
  "Sql": {
    // Selected when ConnectionString is non-empty AND KeyVault:Url is empty.
    // Example for the bundled docker-compose: "Server=localhost,1433;Database=NomaSignDemo;User Id=sa;Password=ChangeMe_Local_Dev_123!;TrustServerCertificate=true;Encrypt=true"
    "ConnectionString": "",
    // 32-byte base64 AES-256 master key. Provide via Sql__MasterKey env var or your
    // secrets manager — never commit the real value.
    "MasterKey": "",
    "KeyVersion": 1
  }
}
